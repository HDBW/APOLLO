<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="De.HDBW.Apollo.Client.Views.Assessments.ModuleOverView"
             xmlns:strings="clr-namespace:De.HDBW.Apollo.Client.Resources.Strings"
             xmlns:models="clr-namespace:De.HDBW.Apollo.Client.Models"
             xmlns:selector="clr-namespace:De.HDBW.Apollo.Client.Selector"
             xmlns:controls="clr-namespace:De.HDBW.Apollo.Client.Controls"
             xmlns:assessmentModels="clr-namespace:De.HDBW.Apollo.Client.Models.Assessment"
             xmlns:viewModels="clr-namespace:De.HDBW.Apollo.Client.ViewModels.Assessments"
             x:DataType="viewModels:ModuleOverViewModel"
             Title="{x:Binding Title, Mode=OneWay}"
             IsBusy="{x:Binding IsBusy, Mode=OneWay}"
             Style="{StaticResource DefaultPageStyle}">
    <ContentPage.Resources>
        <DataTemplate x:Key="TileTemplate" x:DataType="assessmentModels:ModuleTileEntry">
            <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="*, Auto" Padding="8">
                <Border Style="{StaticResource InputBorderStyle}" Grid.ColumnSpan="2" Grid.RowSpan="3">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{x:Binding InteractCommand}" NumberOfTapsRequired="1"/>
                    </Border.GestureRecognizers>
                </Border>
                <Border InputTransparent="True" Style="{StaticResource InputBorderHeaderStyle}" Grid.Column="0" HorizontalOptions="Start">
                    <HorizontalStackLayout InputTransparent="True">
                        <Image InputTransparent="True" WidthRequest="16" HeightRequest="16" IsVisible="{x:Binding HasDecoratorImage, Mode=OneTime}" Margin="0,0,8,0" VerticalOptions="Center">
                            <Image.Source>
                                <FontImageSource Glyph="{x:Binding DecoratorImagePath}" FontFamily="ApolloFontIcons" Size="16" Color="{StaticResource Neutral0}"/>
                            </Image.Source>
                        </Image>
                        <Label InputTransparent="True" Text="{x:Binding Title, Mode=OneWay}" TextColor="{StaticResource Neutral0}" VerticalOptions="Center" Style="{StaticResource Noto12}"/>
                    </HorizontalStackLayout>
                </Border>
                <ImageButton Grid.Column="1" WidthRequest="48" HeightRequest="48" Padding="12" HorizontalOptions="End"
                         Command="{x:Binding ToggleIsFavoriteCommand}"
                         IsVisible="{x:Binding CanBeMadeFavorite}" SemanticProperties.Description="{x:Static strings:Resources.Global_Alttext_MakeFavorite}" SemanticProperties.Hint="{x:Binding Text, Mode=OneTime}"
                         VerticalOptions="Start">
                    <ImageButton.Source>
                        <FontImageSource Glyph="{x:Static models:KnownIcons.MakeFavorite}" FontFamily="ApolloFontIcons" Size="24" Color="{StaticResource Primary}"/>
                    </ImageButton.Source>
                    <ImageButton.Triggers>
                        <DataTrigger TargetType="ImageButton" Binding="{x:Binding IsFavorite}" Value="True">
                            <Setter Property="Source">
                                <Setter.Value>
                                    <FontImageSource Glyph="{x:Static models:KnownIcons.IsFavorite}" FontFamily="ApolloFontIcons" Size="24" Color="{StaticResource Primary}"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="SemanticProperties.Description" Value="{x:Static strings:Resources.Global_Alttext_IsFavorite}" />
                        </DataTrigger>
                    </ImageButton.Triggers>
                </ImageButton>
                <Label InputTransparent="True" Grid.Row="1" Grid.ColumnSpan="2" Text="{x:Binding Text, Mode=OneWay}" Margin="16" TextColor="{StaticResource Primary}" Style="{StaticResource Noto14SemiBold}"/>
                <Label InputTransparent="True" Grid.Row="2" Grid.ColumnSpan="2" Text="{x:Static strings:Resources.Global_TestNow}" Margin="16,0,16,16" TextColor="{StaticResource Primary}" Style="{StaticResource Noto14}"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="IsMemberOnlyTemplate" x:DataType="assessmentModels:ModuleTileEntry">
            <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="*, Auto"  Padding="24,8">
                <Border Style="{StaticResource InputBorderStyle}" Grid.ColumnSpan="2" Grid.RowSpan="3">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{x:Binding InteractCommand}" NumberOfTapsRequired="1"/>
                    </Border.GestureRecognizers>
                </Border>
                <Border InputTransparent="True" Style="{StaticResource InputBorderHeaderStyle}" BackgroundColor="{StaticResource Primary300}" Grid.Column="0" HorizontalOptions="Start">
                    <HorizontalStackLayout InputTransparent="True" >
                        <Image InputTransparent="True" WidthRequest="16" HeightRequest="16" IsVisible="{x:Binding HasDecoratorImage, Mode=OneTime}" Margin="0,0,8,0" VerticalOptions="Center">
                            <Image.Source>
                                <FontImageSource Glyph="{x:Binding DecoratorImagePath}" FontFamily="ApolloFontIcons" Size="16" Color="{StaticResource TextColor}"/>
                            </Image.Source>
                        </Image>
                        <Label Text="{x:Binding Title, Mode=OneWay}" TextColor="{StaticResource TextColor}" VerticalOptions="Center" Style="{StaticResource Noto12}"/>
                    </HorizontalStackLayout>
                </Border>
                <ImageButton Grid.Column="1" WidthRequest="48" HeightRequest="48" Padding="12"
                     Command="{x:Binding ToggleIsFavoriteCommand}"
                     IsVisible="{x:Binding CanBeMadeFavorite}" SemanticProperties.Description="{x:Static strings:Resources.Global_Alttext_MakeFavorite}" SemanticProperties.Hint="{x:Binding Text, Mode=OneTime}"
                     VerticalOptions="Start">
                    <ImageButton.Source>
                        <FontImageSource Glyph="{x:Static models:KnownIcons.MakeFavorite}" FontFamily="ApolloFontIcons" Size="24" Color="{StaticResource Primary}"/>
                    </ImageButton.Source>
                    <ImageButton.Triggers>
                        <DataTrigger TargetType="ImageButton" Binding="{x:Binding IsFavorite}" Value="True">
                            <Setter Property="Source">
                                <Setter.Value>
                                    <FontImageSource Glyph="{x:Static models:KnownIcons.IsFavorite}" FontFamily="ApolloFontIcons" Size="24" Color="{StaticResource Primary}"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="SemanticProperties.Description" Value="{x:Static strings:Resources.Global_Alttext_IsFavorite}" />
                        </DataTrigger>
                    </ImageButton.Triggers>
                </ImageButton>
                <Label InputTransparent="True" Text="{x:Binding Text, Mode=OneWay}" Grid.Row="1" Grid.ColumnSpan="2" Margin="16" Style="{StaticResource Noto14}" TextColor="{StaticResource Primary}"/>
                <HorizontalStackLayout InputTransparent="True" Grid.Row="2" Grid.ColumnSpan="2" Margin="16,0,16,16">
                    <Image InputTransparent="True" WidthRequest="16" HeightRequest="16" IsVisible="{x:Binding HasDecoratorImage, Mode=OneTime}" Margin="0,0,8,0" VerticalOptions="Center" SemanticProperties.Description="{x:Static strings:Resources.AssessmentFeedView_Locked}">
                        <Image.Source>
                            <FontImageSource Glyph="{StaticResource Lock}" FontFamily="ApolloFontIcons" Size="16" Color="{StaticResource Primary}"/>
                        </Image.Source>
                    </Image>
                    <Label InputTransparent="True" Text="{x:Static strings:Resources.Global_RegisterNow}" Style="{StaticResource Noto14SemiBold}" TextColor="{StaticResource Primary}"/>
                </HorizontalStackLayout>
            </Grid>
        </DataTemplate>

        <selector:AssessmentSectionTemplateSelector x:Key="AssessmentSectionTemplateSelector"
                                             IsMemberOnlyTemplate="{StaticResource IsMemberOnlyTemplate}"
                                             TileTemplate="{StaticResource TileTemplate}" />
    </ContentPage.Resources>
    <ScrollView>
        <VerticalStackLayout Padding="16,0,16,24">
            <Label Text="{x:Static strings:Resources.ModuleOverViewModel_Title}" Style="{StaticResource NotoSerif24Bold}" TextColor="{StaticResource Primary}" Margin="8,24,8,16" SemanticProperties.HeadingLevel="Level1"/>
            <Label Text="{x:Static strings:Resources.ModuleOverViewModel_Description}" Style="{StaticResource Noto16}" TextColor="{StaticResource TextColor}" Margin="8,0,8,26"/>
            <Grid ColumnDefinitions="Auto,*,Auto" Margin="8,0,8,16"
              RowDefinitions="Auto, Auto">
                <ImageButton Grid.Column="0" Command="{x:Binding ToggleSegmentSelectionCommand}" CommandParameter="-1" Grid.Row="0" Grid.RowSpan="2" VerticalOptions="Fill" MinimumHeightRequest="48" WidthRequest="44" Padding="10" SemanticProperties.Description="{x:Static strings:Resources.Global_Back}">
                    <ImageButton.Source>
                        <FontImageSource Glyph="{StaticResource ArrowLeft}" FontFamily="ApolloFontIcons" Size="24" Color="{StaticResource Primary}"/>
                    </ImageButton.Source>
                </ImageButton>
                <Label
                    Grid.Column="1" Grid.Row="0" TextColor="{StaticResource Primary}" Style="{StaticResource Noto16SemiBold}"
                    Text="{x:Binding CurrentSegment}"
                    HorizontalTextAlignment="Center"
                    VerticalOptions="Center"
                    HorizontalOptions="Center" />
                <Label
                    Grid.Column="1" Grid.Row="1" TextColor="{StaticResource Primary}" Style="{StaticResource Noto16}"
                    Text="{x:Binding CurrentQuantity}"
                    VerticalOptions="Center"
                    HorizontalOptions="Center" />
                <ImageButton Grid.Column="2" Command="{x:Binding ToggleSegmentSelectionCommand}" CommandParameter="1" Grid.Row="0" Grid.RowSpan="2" VerticalOptions="Fill" MinimumHeightRequest="48" WidthRequest="48" Padding="12" SemanticProperties.Description="{x:Static strings:Resources.Global_Next}">
                    <ImageButton.Source>
                        <FontImageSource Glyph="{StaticResource ArrowRight}" FontFamily="ApolloFontIcons" Size="24" Color="{StaticResource Primary}"/>
                    </ImageButton.Source>
                </ImageButton>
            </Grid>
            
            <controls:SegmentSvgImage Margin="8,0,8,24"
                                      InputTransparent="True"
                                      x:Name="PART_Image"
                                      HeightRequest="280"
                                      IsShowingModulResult="True"
                                      Segments="{x:Binding Segments}" />
            <VerticalStackLayout Margin="0"
                BindableLayout.ItemsSource="{x:Binding Modules}"
                BindableLayout.ItemTemplateSelector="{StaticResource AssessmentSectionTemplateSelector}"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
