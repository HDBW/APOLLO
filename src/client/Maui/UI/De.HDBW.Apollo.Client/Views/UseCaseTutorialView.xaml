<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="De.HDBW.Apollo.Client.Views.UseCaseTutorialView"
             NavigationPage.HasBackButton="False"
             NavigationPage.HasNavigationBar="False"
             xmlns:viewModels="clr-namespace:De.HDBW.Apollo.Client.ViewModels"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:skia="clr-namespace:SkiaSharp.Extended.UI.Controls;assembly=SkiaSharp.Extended.UI"
             xmlns:strings="clr-namespace:De.HDBW.Apollo.Client.Resources.Strings"
             IsBusy="{Binding IsBusy}"
             x:DataType="viewModels:UseCaseTutorialViewModel">
    <Grid RowDefinitions="*, Auto">
        <skia:SKLottieView x:Name="PART_Animation" Source="animation.json" Grid.Row="0" RepeatMode="Restart" RepeatCount="-1" IsAnimationEnabled="True"  VerticalOptions="Fill" HorizontalOptions="Fill"/>
        <Button Grid.Row="1" Text="{x:Static strings:Resource.Global_Skip}" Command="{Binding SkipCommand}" />
    </Grid>
</ContentPage>
